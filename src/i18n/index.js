import es from './es';
import en from './en';
import de from './de';

export const translations = {
  es,
  en,
  de
};

export const availableLanguages = ['es', 'en', 'de'];

export default translations;
